- 所有数据库操作函数均接收 `&SqlitePool` 作为第一个参数，并返回 `Result<T, sqlx::Error>` 或自定义 `AppError`。
- API 响应统一通过 `ApiResponse` 辅助结构体包装，成功请求返回包含 `data` 字段的 JSON 对象。
- 敏感数据（如 Token 明文）在列表查询时通过 `ApiTokenInfo` 等专用 DTO 结构体进行脱敏处理，不直接暴露内部模型。
- 中间件通过 `request.extensions_mut().insert()` 将认证后的用户信息注入请求上下文，供下游 Handler 提取。