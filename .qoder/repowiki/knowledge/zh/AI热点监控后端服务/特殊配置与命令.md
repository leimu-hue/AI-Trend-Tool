- **多模式启动**：支持 `cargo run -- [mode]`，其中 mode 可选 `all`, `api`, `parser`, `filter`, `pusher`。
- **Docker 构建**：提供多阶段 Dockerfile，第一阶段编译 Rust 二进制，第二阶段基于 Debian Slim 运行，优化镜像体积。
- **自动迁移**：首次启动时自动执行 `docs/migrations` 下的 SQL 脚本初始化数据库结构。